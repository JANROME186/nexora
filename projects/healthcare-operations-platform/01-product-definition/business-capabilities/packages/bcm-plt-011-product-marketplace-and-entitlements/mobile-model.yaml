artifact:
  id: HOP-MOB-BCM-PLT-011
  type: mobile-model
  name: Product Marketplace and Entitlements Mobile Model
  version: 1.0.0
  status: modeled
  backlog_item: COM-MOD-017-DEF

mobile_scope: entitlement_aware_consumption
flows:
  - id: MOB-MKT-001
    name: Entitlement-aware feature availability
    surfaces: [mobile_app]
    behavior: Mobile app reads package feature availability from generated entitlement summary.
  - id: MOB-MKT-002
    name: Package unavailable state
    surfaces: [mobile_app]
    behavior: User sees localized unavailable or contact-admin state without exposing unauthorized routes.

offline_behavior:
  entitlement_cache: read_only_short_lived
  activation_commands_offline: not_allowed

