artifact:
  id: HOP-GEN-BCM-PLT-007
  type: generation-plan
  name: Audit Trail Generation Plan
  version: 1.0.0

generatable_outputs:
  backend:
    - AuditEvent entity, repository, and service skeleton.
    - REST controller endpoints matching openapi-source.yaml.
  frontend:
    - Audit log search and export UI components.

custom_implementation_points:
  - Cryptographic SHA-256 hash chaining interceptor.
  - Asynchronous non-blocking audit event writer with persistent queue buffer.
