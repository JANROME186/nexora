artifact:
  id: HOP-EVT-BCM-INV-005
  type: events
  name: Stock Entries Events
  version: 0.1.0
  status: modeled
  classification: editable_model
  capability: BCM-INV-005

domain_events:
  - name: StockReceived
    description: A stock entry increased on-hand quantity for an InventoryItem/StockLot.
    payload: [stockEntryId, inventoryItemId, stockLotId, quantity, sourceType]
    audit: true

integration_events:
  published:
    - name: StockReceived
      description: Signals BCM-INV-004 that a purchase order line's goods were received.
      consumers: [inventory-procurement]
  consumed: []

published_language: []
