artifact:
  id: HOP-TRACE-BCM-PLT-008
  type: traceability
  name: Document Management Traceability
  version: 0.1.0
  status: modeled
  classification: editable_model
  capability: BCM-PLT-008

traces:
  capability_map:
    bcm_001: BCM-PLT-008
    domain: DOM-10 Platform
  dependency_map:
    bcm_002_profile: platform_extension
    required_capabilities: [BCM-PLT-001, BCM-PLT-007]
    downstream_capabilities: [BCM-RES-002]
  domain_foundation:
    bounded_context: document-management
    aggregate_reference: StoredDocument (new platform aggregate; referenced_by Patient/LaboratoryResult/Invoice/ImagingStudy per BCM-002)
    context_relationships: []
    context_map_observation: >-
      context-map.yaml does not yet declare a formal relationship entry for document-management;
      BCM-002's capability-dependency-map.yaml already names it as a related_bounded_context for
      BCM-RES-002 and BCM-PLT-008. Formalizing a context-map.yaml relationship entry is an
      architecture-governance action requiring an ADR and is out of scope for this definition-only
      backlog item; recorded here as a non-blocking traceability observation, not a modeling defect.
    shared_kernel_refs: [VO-ID-001, VO-ID-002, VO-007]
  brm_alignment:
    - rule: BRM-001-R018
      alignment: Every store, retrieval-mismatch and disposal-scheduling event is audited (RN-001, RN-002, RN-004).
  hrp_alignment:
    - process: HRP-001-P07 Result Report and Digital Delivery
      capability_role: Store report document segment of the process.
  rules_to_tests:
    - rule: RN-001
      tests: [TST-DOC-008-01]
    - rule: RN-002
      tests: [TST-DOC-008-02]
    - rule: RN-003
      tests: [TST-DOC-008-03]
    - rule: RN-004
      tests: [TST-DOC-008-04]
    - rule: RN-005
      tests: [TST-DOC-008-05]
    - rule: RN-006
      tests: [TST-DOC-008-06]
  processes_to_commands:
    - process: PRC-DOC-008-01
      commands: [StoreDocument]
    - process: PRC-DOC-008-03
      commands: [ScheduleDocumentDisposal]
  api_to_permissions:
    - operation: storeDocument
      scope: document.store
    - operation: getDocument
      scope: document.read
    - operation: scheduleDocumentDisposal
      scope: document.store
  events_to_audit:
    - event: DocumentStored
      audit_sink: BCM-PLT-007
    - event: DocumentIntegrityMismatchDetected
      audit_sink: BCM-PLT-007
    - event: DocumentDisposalScheduled
      audit_sink: BCM-PLT-007
  ui_to_api: []
  consumed_by_capabilities:
    - capability: BCM-RES-002
      relationship: Persists GeneratedResultReport bytes through StoreDocument and retrieves them through GetDocument.
  generated_outputs_ref: generation-plan.yaml
  qa_evidence: ../../../../08-qa/qa/results-and-digital-delivery/MVP-MOD-007-DEF-validation.yaml
  backlog_items:
    definition: MVP-MOD-007-DEF
    definition_status: modeled
    compilation: MVP-MOD-007-BE-001
    compilation_status: closed
    custom_rules: MVP-MOD-007-BE-002
    custom_rules_status: closed
    ui: MVP-MOD-007-FE-001
    ui_status: not_applicable
    validation: MVP-MOD-007-QA-001
    validation_status: pending
    closeout: MVP-MOD-007-CLOSEOUT
    closeout_status: pending

