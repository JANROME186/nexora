artifact:
  id: HOP-TST-BCM-PLT-002
  type: test-model
  name: Platform Configuration Test Model
  version: 1.0.0

test_cases:
  - id: TEST-CFG-001
    name: Verify Feature Flag Evaluation Under Tenant Override
    type: unit
    target_rule: RN-CFG-001
    expected_result: Evaluator returns tenant-specific flag status.

  - id: TEST-CFG-002
    name: Verify Tenant PII Masking Rule Resolution
    type: unit
    target_rule: RN-CFG-002
    expected_result: Document and credential read models are masked according to tenant rule (addressing TD-BE-008).

  - id: TEST-CFG-003
    name: Verify Encrypted Config Persistence
    type: security
    target_rule: RN-CFG-003
    expected_result: Raw values in database remain encrypted at rest.
