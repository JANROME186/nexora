# ENT-001 Patient

Patient is a master-data entity representing a person receiving diagnostic services.

The entity is tenant-scoped and must support auditability, privacy controls and duplicate-prevention strategies.

See `patient.yaml` for the machine-readable model.
