openapi: 3.1.0
info:
  title: Nexora Example API
  version: 0.1.0
  description: Contract-first API definition.
servers:
  - url: https://api.nexora.local/v1
paths: {}
components:
  schemas:
    ErrorResponse:
      type: object
      required: [code, message]
      properties:
        code:
          type: string
        message:
          type: string
