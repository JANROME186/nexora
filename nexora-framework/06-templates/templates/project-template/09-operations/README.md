# 09 Operations

Place deployment, local runtime, observability and runbooks here.

Required integrated local solution runbook:

- `runbooks/local-solution-runbook.md`
- `runbooks/local-solution-runbook.yaml`

The integrated runbook must explain how a human reviewer starts, validates and stops the complete local solution without opening each component folder manually.
